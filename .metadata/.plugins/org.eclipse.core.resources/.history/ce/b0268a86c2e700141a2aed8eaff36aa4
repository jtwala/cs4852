
public class Network {
	
	private AdjMatrixGraph tempMatrix;

	/**Run one 'round' of the network algorithm on the given graph,
	 * modifying in place.
	 */
	public void simulateRound(AdjMatrixGraph G) {
		int v = G.V();
		tempMatrix = new AdjMatrixGraph(G.V());
		for (int i = 0; i < v; i++) {
			for (int j = 0; j < v; j++){
				
			}
		}
		
	}
	
	private void calculateNodeStatus(AdjMatrixGraph G, int node) {
		
	}
	
    //Test client
    public static void main(String[] args) {
        int V = 4;
        int E = 7;
        AdjMatrixGraph G = new AdjMatrixGraph(V, E);
        System.out.println(G);
    }

}
